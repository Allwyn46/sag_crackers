import { Digitsonly } from './digitsonly';

describe('Digitsonly', () => {
  it('should create an instance', () => {
    const directive = new Digitsonly();
    expect(directive).toBeTruthy();
  });
});
